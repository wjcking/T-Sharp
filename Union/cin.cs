#define -u-dwf~1
#define -e
using System;

union truct c
{
	public byte bit;
	//COLOR~BYTE FST^E TW^Q BTE^YIN~A
	public byte its()
	{
		bit = bit<= Byte.MinValue || bit >= Byte.MaxValue;
		this.bit = its();//<Tint, Tchar>
		return bit;
	}
	//this^ = |
	tatic<-->
	{
		/*Tchar = default(Tint);
		Tint = default(Tchar);
		
		cin.its<Tchar>();
		cin.its<Tint>();*/
		// bit = i.bit;
	}
}