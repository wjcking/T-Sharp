struct i
{
	public byte bit; // 1 = Tint, 2 = Tchar bit
	
	//^thisqut = |
	public static i operator++ (i t)
	{
		Tint = default(Tchar);
		Tchar = default(Tint);

		//this.its<Tint>();
		//this.its<Tchar>();
	}
	
}